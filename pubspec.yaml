name: clean_arc
description: A Clean Architecture generator for Flutter projects
version: 1.0.0
repository: 'https://github.com/Invincible1996/clean_arc.git'

environment:
  sdk: '>=3.0.0 <4.0.0'

# Add regular dependencies here.
dependencies:
  args: ^2.4.2
  path: ^1.8.3
  yaml: ^3.1.2
  collection: ^1.18.0
dev_dependencies:
  test: ^1.24.9

executables:
  clean_arc: main
